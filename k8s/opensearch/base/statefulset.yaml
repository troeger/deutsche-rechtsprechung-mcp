apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: court-decisions-mcp-opensearch
spec:
  serviceName: court-decisions-mcp-opensearch
  replicas: 1
  selector:
    matchLabels:
      app: court-decisions-mcp-opensearch
  template:
    metadata:
      labels:
        app: court-decisions-mcp-opensearch
    spec:
      securityContext:
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: opensearch
          image: opensearchproject/opensearch:2
          args:
            - "-Ediscovery.type=single-node"
          env:
            - name: OPENSEARCH_INITIAL_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: court-decisions-mcp-opensearch
                  key: OPENSEARCH_ADMIN_PASSWORD
            - name: OPENSEARCH_JAVA_OPTS
              value: "-Xms1g -Xmx1g"
          ports:
            - name: https
              containerPort: 9200
            - name: transport
              containerPort: 9300
          volumeMounts:
            - name: court-decisions-mcp-opensearch
              mountPath: /usr/share/opensearch/data
  volumeClaimTemplates:
    - metadata:
        name: court-decisions-mcp-opensearch
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 20Gi
