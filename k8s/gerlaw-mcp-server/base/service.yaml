apiVersion: v1
kind: Service
metadata:
  name: court-decisions-mcp-server
spec:
  selector:
    app: court-decisions-mcp-server
  ports:
    - name: http
      port: 8002
      targetPort: 8002
  type: ClusterIP
